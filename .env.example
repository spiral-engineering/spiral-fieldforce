# Copy this file to .env and fill in your values.
# .env is git-ignored; .env.example is committed.

# API base URL (defaults to production if not set)
REACT_APP_API_BASE_URL=https://api.spiral-fieldforce.com/v1
